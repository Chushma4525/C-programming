#include<stdio.h>
#include<coio.h>
int fact(int);
int fact(inta)
{
  if(a==1)
    return 1;
else
return a*fact(a-1);
}
void main()
{
 int a;
printf("Enter any integer");
scanf("%d",&a);
printf("The factorial is %d",fact(a));
}
